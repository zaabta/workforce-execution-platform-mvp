/** Permission codes that govern the Crew Management feature (SDD 12.3). */
export const CREW_PERMISSIONS = {
  CREATE: 'crew.create',
  ASSIGN_WORKER: 'crew.assign_worker',
  REMOVE_WORKER: 'crew.remove_worker',
} as const;
